name: Deploy to Fly.io

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
<<<<<<< HEAD:ubuntu_hosts/.github/workflows/deploy.yaml
=======
    name: deploy

    defaults:
      run:
        working-directory: ubuntu_hosts

>>>>>>> 8ec17a12ef2f64803a72c29591c28ecab5eede82:.github/workflows/deploy.yaml
    steps:
      - uses: actions/checkout@v4

      - uses: superfly/flyctl-actions/setup-flyctl@master

      - run: flyctl deploy --remote-only
        env:
          FLY_API_TOKEN: ${{ secrets.FLY_API_TOKEN }}